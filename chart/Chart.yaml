# Scaffold so `helm template` can render migration-job.yaml on its own.
# Copy templates/migration-job.yaml into your real chart; this file is not part
# of it.
apiVersion: v2
name: keep-migration-job-reference
description: Reference PreSync migration Job template
version: 0.1.0
appVersion: "1.0.0"
